# Session 000025 — Fix: ícones Facebook/Instagram SVG, responsividade mobile, instagram icon

## Date
2026-06-28

## Task
1. Fix Facebook and Instagram icons — PNGs 22x22 were rendering empty (blank/filtered)
2. Fix Instagram icon appearance and mobile responsiveness

## Work done

### 1. Facebook and Instagram icons — SVG replacement
The PNG icons (22x22) were rendering as blank/empty squares because the CSS `filter: invert()` was removing all color from the dark PNGs on the dark background, making them invisible.
- Replaced `facebook-icon.png` and `instagram-icon.png` usage with `facebook-icon.svg` and `instagram-icon.svg`
- SVGs are vector, support fill color, and don't need filter hacks
- The existing SVGs (`facebook-icon.svg`, `instagram-icon.svg`) were already in the images directory

### 2. Instagram icon fix + mobile responsiveness
- Ensured Instagram icon renders correctly across all breakpoints
- Fixed mobile responsiveness for the social icons row in the footer
- Icons now display properly on all screen sizes

## Files modified
- `images/facebook-icon.svg` (source + deploy) — created/fixed
- `images/instagram-icon.svg` (source + deploy) — created/fixed
- `footer.php` (source + deploy) — updated icon references
- `style.css` (source + deploy) — fixed icon sizing and mobile layout

## Git commits
- `fa70125` — "fix: facebook e instagram icons SVG (PNGs 22x22 ficavam vazios com filter)"
- `6f1d29a` — "fix(ui): corrige icone instagram e responsividade mobile"
