# Session 000053 — .env infrastructure, Telegram working, Meta App + Threads App registration

## Date
2026-07-02

## Task
1. Create load_env() + trex_get_credential() system for reading .env
2. Store all social media credentials in .env (prioritário) with fallback to wp-options.json
3. Guide user to create Telegram bot and obtain token
4. Test Telegram posting — working with photo upload via CURLFile
5. Guide user to create Meta Developer App and register
6. Guide user to register Threads App

## Work done

### .env credential system
- Created `load_env()` in wp-load.php — reads .env file and defines PHP constants
- Created `trex_get_credential($key)` — checks .env constant first, falls back to get_option()
- Updated all 6 sharing functions (Telegram, Facebook, Instagram, TikTok) to use trex_get_credential()
- Updated API options endpoint to show ***configured*** instead of raw tokens
- Updated admin.php to show ✓ .env badge when credential is set via .env
- Admin form fields kept as fallback for editing via GUI

### Telegram integration
- User created bot via @BotFather on Telegram
- Bot Token and Chat ID saved to .env
- Fixed `sendPhoto` to use `CURLFile` for local file upload (server is local, not publicly accessible)
- Added `trex_url_to_local_path()` helper — maps image URL to local file path
- Tested successfully: message sent with photo (1536x1074) + title + lead + URL

### Meta App registration
- User registered as Facebook Developer (needed credit card verification)
- Created Meta App:
  - APP_ID = 1791222012241978
  - APP_SECRET = cfd8d841b18bc29853b334a4aa350758
- Saved to .env
- User now has access to WhatsApp, Threads, Instagram, Messenger, Facebook via Business Manager

### Threads App registration
- Threads APP_ID = 3313315462186427
- Threads APP_SECRET = 7723fa2577d6ccef5652bc35a869858d
- Saved to .env

## Credentials stored in .env
- `TELEGRAM_BOT_TOKEN` — Telegram bot token
- `TELEGRAM_CHAT_ID` — Telegram chat ID
- `META_APP_ID` — Meta/Facebook Developer App ID
- `META_APP_SECRET` — Meta/Facebook Developer App Secret
- `THREADS_APP_ID` — Threads Developer App ID
- `THREADS_APP_SECRET` — Threads Developer App Secret

## Pending
- Get Facebook Page Access Token via Graph API Explorer
- Get Instagram Business Token + ID (connected to Facebook Page)
- Get TikTok Access Token
- Implement Threads posting function
- Test all platforms

## Files modified
- `.env` (source + deploy) — added TELEGRAM, META, THREADS credentials
- `wp-load.php` (source + deploy) — added load_env(), trex_get_credential(), trex_url_to_local_path(), updated all sharing functions, updated /api/v1/options
- `index.php` (source + deploy) — updated /api/v1/options to show ***configured***
- `wp-content/themes/tiranossaurusrex/admin.php` (source + deploy) — added ✓ .env badges, info text about credential priority
