# Session 000050 — Verify posts 10-40, remove attachment/banner fallback, find 1 more Wix image

## Date
2026-07-01

## Task
1. Check posts 10-40 on live Wix site, verify each featured image is correct
2. Fix any wrong images
3. Remove placeholder/banner fallback for posts without Wix images (user: "placeholders devem ser deletados")
4. Ensure ad insertion pattern (5 posts + 1 ad) is working correctly

## Work done

### 1. Verified posts 10-40 (31 posts) against live Wix site
Downloaded the blog-posts-sitemap.xml fresh, then for each post ID 10-40:
- Found the Wix slug via sitemap image hash matching
- Fetched the live Wix post page
- Extracted og:image URL
- Compared the image hash (7aff73_HASH) with what's stored locally

**Result: 29 of 31 posts have CORRECT Wix images.** The hashes match the live Wix site exactly. The "MISMATCH" earlier was a false alarm — Wix's og:image includes transform paths (`/v1/fill/w_2500...`) but the image hash is identical.

| Status | Count | IDs |
|---|---|---|
| Correct Wix image | 29 | 10-29, 31-33, 35-40 |
| No Wix image (deleted from Wix) | 2 | 30, 34 |

### 2. Found 1 additional Wix image (ID 894)
- ID 894 ("O inferno não existe, sua igreja está mentindo para você!") was found on the live Wix site via direct slug matching
- Found image: `7aff73_0cde8beadb4147538fc509f366d59dc6`
- Downloaded to local filesystem, updated `_image_mapping.json` and `_wix_files.json`
- Total Wix images: 929 files, 1127/1163 posts matched (96.97%)

### 3. Fixed `get_post_by_id()` to include Wix image URL
The `/api/v1/posts/{id}` endpoint was returning raw post data without `featured_image_url`. Added Wix image lookup to `get_post_by_id()`.

### 4. Removed ALL attachment/banner fallback code (user directive)
The user said placeholders for posts without Wix images should be DELETED — no fake images, no default banner, nothing.

**Files modified:**
- **`has_post_thumbnail()`** — removed `|| !empty($post['featured_media'])` fallback. Now only returns true if `featured_image_url` is set from Wix.
- **`the_post_thumbnail()`** — removed entire attachment lookup branch (`get_media_embedded_url`). Now only renders Wix images.
- **`trex_get_posts_json()`** — removed `$default_img` (banner URL) and `get_media_embedded_url()` fallback. Now sets `featured_image_url` to empty string if no Wix image.
- **`index.php`** — removed the `<img src="...Banner_para_o_Blog...">` fallback inside the `else` branch. Now the `<div class="post-card-image">` renders empty if no thumbnail.
- **`get_media_embedded_url()`** — removed entirely (dead code, now unused).

### 5. Verified ad insertion pattern
Confirmed the pattern `1,2,3,4,5,AD,6,7,8,9,10,AD,...` — every 5th post card has `class="post-card ad-card"`. No changes needed, was already correct.

## Verification
- API returns Wix images for posts with them, empty `featured_image_url=""` for posts without
- Single page for ID 30 shows NO post-image section (not even empty div)
- Homepage shows Wix images for all 12 initial posts
- `has_post_thumbnail()` returns false for IDs 30, 34 → no broken images
- Ad cards appear at positions 6 and 12 (every 5 posts)

## Files modified
- `wp-load.php` (source + deploy) — get_post_by_id() fix, has_post_thumbnail/the_post_thumbnail/trex_get_posts_json fallback removal, get_media_embedded_url() removed
- `wp-content/themes/tiranossaurusrex/index.php` (source + deploy) — removed banner fallback img tag
- `_image_mapping.json` (source + deploy) — added ID 894 mapping
- `_wix_files.json` (source + deploy) — added new wix image entry
- `wp-content/themes/tiranossaurusrex/images/wix_7aff73_0cde8beadb4147538fc509f366d59dc6.jpg` (source + deploy) — new image for ID 894
