## Session 000023 — Correções finais: 12 cards, hamburger, foto Wix, remover redes sociais

Date: 26 Jun 2026

### Tasks executadas

1. **12 cards iniciais** — `itemsPerLoad` alterado de 9 para 12 em `main.js` (linha 179).
2. **Remover redes sociais do cabeçalho** — Bloco `.header-social` removido de `header.php` e todo CSS `.header-social` removido de `style.css`.
3. **Corrigir menu sanduíche mobile** — Substituído `NodeList.forEach()` por loop `for` tradicional em `initColorCycling()` e `initHamburger()` no `main.js`. Isso evita TypeError em navegadores mobile que não suportam `NodeList.prototype.forEach` (Android WebView, Safari antigo).
4. **Primeiro card sem foto** — Baixada a imagem real do Wix (primeiro post "Vice-presidente da AMMA...") de `https://static.wixstatic.com/media/7aff73_948352d619fe4b7d88174cb099ec985c~mv2.jpeg` para o arquivo `vice-presidente-da-AMMA-Senador-Canedo-é-finalista-do-Prêmio-Espírito-Público-2025.jpg` (158KB). O `index.php` já referencia esta imagem para o `$post_index === 1`.

### Pendências
- Nenhuma.

### Arquivos modificados
| Arquivo | Mudança |
|---------|---------|
| `header.php` | Removido `.header-social` |
| `style.css` | Removido bloco `header-social` CSS |
| `js/main.js` | `itemsPerLoad: 9→12`, `forEach→for` loop |
| `images/vice-presidente-...jpg` | Substituída pela imagem real do Wix |
