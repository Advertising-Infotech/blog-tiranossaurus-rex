# SESSION 000014 — Rede local, publicidade, AdSense, API fallback, página temporária

## Tasks Completed

### 1. Servidor em Rede Local
- Server bind alterado de `localhost:54549` para `0.0.0.0:54549`
- `WP_HOME` em `wp-load.php` agora auto-detecta `$_SERVER['HTTP_HOST']`
- Firewall rule do Windows criada: `Tiranossaurus Rex Server` (porta 54549, inbound)
- Scripts atualizados: `iniciar-servidor.bat`, `ligar-server.ps1`, `start.bat`
- Acessível em `http://192.168.1.66:54549/` na rede local

### 2. Auto-start com Windows
- Criado `startup.vbs` em `C:\trex\` (roda PHP server invisível em background)
- Scheduled task criada: `TiranossaurusRexServer` (executa no logon do Windows)

### 3. Banner Rotativo 728x90
- Adicionado em `header.php` abaixo da nav
- 3 slides: `Publicidade_cabeçalho.gif`, `Banner_para_o_Blog_Tiranossaurus_Rex.jpg`, placeholder "ANUNCIE AQUI"
- Rolagem automática direita→esquerda via CSS `@keyframes bannerScroll` (15s cycle)
- Pausa no hover
- Placeholder estilizado (onerror) quando imagem não existe

### 4. Segundo Espaço Publicitário
- Abaixo do banner rotativo: `ad-space-secondary` com 468px max-width
- Exibe `Banner_para_o_Blog_Tiranossaurus_Rex.jpg`

### 5. Google AdSense Cards (6º card)
- Em `index.php`, a cada 6 posts (counter `% 6 === 0`), insere um `article.post-card.ad-card`
- Placeholder com label "Publicidade" + espaço para AdSense
- Configurável via Admin page (Publisher ID + Ad Slot)

### 6. Página Temporária (Em Construção)
- Criado `em-construcao.php` — template sem header/footer próprio (incluído via `single.php`)
- Mostra: 🚧 "Este site está sendo reconstruído..."
- `single.php` usa `get_post_by_id()` — se post não existe, mostra construção
- Router `index.php` consertado: `?p=` agora checa ANTES de `/`

### 7. Admin Page
- Criado `admin.php` tema em `/admin`
- Formulário para: AdSense Publisher ID, Ad Slot ID, Chaves de API
- POST salva em `wp-options.json` via `update_option()`
- Roteamento tratado no `index.php` raiz

### 8. API Fallback System
- Criado `js/api-fallback.js` — suporte para 15+ provedores de API
- Fallback automático: tenta cada chave em cada endpoint sequencialmente
- Provedores: OpenAI, Claude, Gemini, DeepSeek, Mistral, Groq, Together, Perplexity, Cohere, AI21, HuggingFace, Replicate, OpenRouter, Fireworks, Lepton
- Status bar no rodapé da página indicando estado da API
- Chaves armazenadas no `localStorage`
- Interface `window.TREX_API.query(prompt, onChunk)`

### 9. Correções na Base
- `wp-load.php`: adicionado `get_post_by_id()`, `rewind_posts()`, `locate_template()`, `esc_attr()`
- Router `index.php`: ordem corrigida (`?p=` antes de `/`)

## Files Created/Modified
- `C:\trex\startup.vbs` — Auto-start invisível
- `wordpress/wp-content/themes/tiranossaurusrex/admin.php` — Admin page
- `wordpress/wp-content/themes/tiranossaurusrex/em-construcao.php` — Página temporária
- `wordpress/wp-content/themes/tiranossaurusrex/js/api-fallback.js` — API fallback engine
- `wordpress/wp-content/themes/tiranossaurusrex/header.php` — Banner ads adicionados
- `wordpress/wp-content/themes/tiranossaurusrex/index.php` — AdSense cards no loop
- `wordpress/wp-content/themes/tiranossaurusrex/single.php` — Construção p/ posts não-migrados
- `wordpress/wp-content/themes/tiranossaurusrex/functions.php` — API fallback enqueue
- `wordpress/wp-content/themes/tiranossaurusrex/style.css` — CSS para banners, ads, admin, construção, API
- `wordpress/wp-load.php` — get_post_by_id, rewind_posts, locate_template, esc_attr
- `wordpress/index.php` — Router corrigido + admin POST handler
- `iniciar-servidor.bat`, `ligar-server.ps1`, `start.bat` — Bind 0.0.0.0

## Test Results
- Homepage: 259 matches (banners, cards, ads) ✓
- Post page (p=1): mostra conteúdo real ✓
- Construction page (p=9999): "Em Construção" ✓
- Admin page (/admin): "Painel de Administração" ✓
- Zero PHP errors ✓
- Zero syntax errors ✓
- Rede local: `http://192.168.1.66:54549/` ✓

## Pendente para Próxima Sessão
- Admin page completa (UI refinada + mais configs)
- Campo de busca com IA conectado ao fallback
- Scrapping de ~1104 posts restantes
- Download de imagens faltantes
- Git commit & push
